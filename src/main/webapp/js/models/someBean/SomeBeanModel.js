define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone){

    var SomeBeanModel = Backbone.Model.extend({
        initialize: function (options) {
        }
    });

    return SomeBeanModel;
});
